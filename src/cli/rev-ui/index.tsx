#!/usr/bin/env node
import React from 'react';
import { render } from 'ink';
import { RevolutionaryUI } from './App.js';

// Clear the console and render the full-screen app
console.clear();
render(<RevolutionaryUI />);